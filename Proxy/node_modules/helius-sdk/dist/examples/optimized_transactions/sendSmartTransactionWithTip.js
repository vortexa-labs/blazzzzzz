"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const src_1 = require("../../src"); // Replace with 'helius-sdk' in a production setting
const web3_js_1 = require("@solana/web3.js");
function main() {
    return __awaiter(this, void 0, void 0, function* () {
        const helius = new src_1.Helius("YOUR_API_KEY");
        const fromKeypair = web3_js_1.Keypair.generate(); // Replace with your loaded keypair
        const fromPubkey = fromKeypair.publicKey;
        const toPubkey = new web3_js_1.PublicKey("TARGET_WALLET_ADDRESS"); // Replace with the recipient address
        const instructions = [
            web3_js_1.SystemProgram.transfer({
                fromPubkey,
                toPubkey,
                lamports: 0.05 * web3_js_1.LAMPORTS_PER_SOL,
            }),
        ];
        // If you're using address lookup tables, load them here
        const addressLUTs = [];
        const bundleId = yield helius.rpc.sendSmartTransactionWithTip(instructions, [fromKeypair], // Signers
        addressLUTs, // Address lookup tables
        100000, // Tip amount (in microLamports)
        "NY" // Region
        );
        console.log(`Bundle sent successfully with ID: ${bundleId}`);
    });
}
main().catch((err) => {
    console.error("Example failed:", err);
    process.exit(1);
});
